nextflow_pipeline {

    name "Test Workflow main.nf"
    script "../main.nf"
    config "../conf/tests/test_panel.config"

    test("Test case scenario: Panel, tumor-only. 1 samples tested with --submussion_basepath=panel --fasta=mini.fa.gz target=mini.genes.bed") {

        when {
            params {
                outdir = "${outputDir}"
            }
        }

        then {
            def stable_path = getAllFilesFromDir(params.outdir, relative:true, ignoreFile: 'tests/.nftignore')
            def report = path("${params.outdir}/report.csv")

            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    // Number of successful tasks
                    workflow.trace.succeeded().size(),
                    // All stable path name, with a relative path
                    stable_path,
                    // All files with stable contents
                    report
                ).match() }
            )
        }

    }

}
